#include <iostream>
#include "Prototipos.h"

using namespace std;

int main(){
    /*
    int ***x;
    int *dim;
    dim=dimensio_variables(3,5);
    x=generar_matrices(3,5);

    *(dim+1)=3;

    cambiar_dimension_matriz(x,dim,1,5);

    cout<<"impresion despues de cambiar: "<<endl;
    for (int i = 0; i <3; ++i) {
        for (int j = 0; j < *(dim+i); ++j) {
            for (int k = 0; k < *(dim+i); ++k) {
                cout <<  *(*(*(x+i)+j)+k) << " ";
            }
            cout << endl;
        }
        cout << endl;
    }

    cout<<"Rotacion "<<endl;

    *(dim+2)=7;

    cambiar_dimension_matriz(x,dim,2,5);
    rotations(x,7,2,2 );

    for (int i = 0; i <3; ++i) {
        for (int j = 0; j < *(dim+i); ++j) {
            for (int k = 0; k < *(dim+i); ++k) {
                cout <<  *(*(*(x+i)+j)+k) << " ";
            }
            cout << endl;
        }
        cout << endl;
    }
*/
}
